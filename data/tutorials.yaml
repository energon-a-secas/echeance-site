# Renewal tutorials: the real steps to obtain, renew and revoke each
# kind of credential the fleet depends on. One entry per tutorial.
#
# This file feeds two consumers:
#   - the Tutorials view in the site (fetched at runtime)
#   - llms.txt at the site root (regenerate with `make llms`)
# Edit here, never in llms.txt.
#
# Rules for entries: steps and insights carry commands and traps, never
# secret values, never IP addresses, never account emails.

tutorials:
  - id: convex-cli
    title: Renew Convex CLI auth
    service: Convex
    applies_to: oauth-session
    signals:
      - "`npx convex deploy` or `npx convex dev` fails with an authentication error"
      - "Convex deploys have been blocked since the last login went stale"
    steps:
      - "Diagnose first, and do not assume expiry: `npx convex login status` prints the account's reachable teams. If the team listed is not the one that owns the project, the token is fine and belongs to the WRONG ACCOUNT, which is a different fix."
      - "Open a real interactive terminal. The login is a browser OAuth flow; it cannot run from a non-interactive session, so an agent must hand this step to a person."
      - "From the Convex project directory (for the fleet: `projects/memes-site`), run `npx convex login --force`."
      - "Complete the browser prompt with the account that owns the project's team, not merely any Convex account."
    verify: "`npx convex dev --once` from the project directory completes without an auth error."
    revoke: "Log out with `npx convex logout`, or revoke the session from the Convex dashboard settings."
    insights:
      - "`npx convex login` SHORT-CIRCUITS. With any token already in ~/.convex/config.json it answers 'this device has previously been authorized and is ready for use' and changes nothing, without checking which account that token belongs to. Only `--force` re-authorizes. This cost two sessions in 2026-09."
      - "'You don't have access to the selected project' is an ACCOUNT mismatch, not an expiry. Convex says the same thing whether the project moved, was deleted, or your token is for another account; `login status` is what tells them apart."
      - "The fleet's own entry claimed a ~90 day token lifetime, inferred from the config file's date. It was wrong: the token had not expired at all. Do not infer a lifetime from a file mtime."
      - "Renewal is the one step in the fleet's deploy chain that cannot be automated; schedule it, do not discover it."
    links:
      - label: Convex dashboard
        url: https://dashboard.convex.dev/

  - id: wrangler-oauth
    title: Renew the Cloudflare wrangler session
    service: Cloudflare
    applies_to: oauth-session
    signals:
      - "`wrangler deploy` or `wrangler secret put` fails with an auth error"
    steps:
      - "Check who the CLI thinks you are: `wrangler whoami`."
      - "If the session is stale, run `wrangler login` (browser OAuth; needs an interactive terminal)."
      - "Worker secrets are written from the worker's own directory with `wrangler secret put NAME`; `worker/wrangler.toml` names the worker."
    verify: "`wrangler whoami` reports the expected account."
    revoke: "`wrangler logout`, or revoke the authorization under the Cloudflare dashboard's API tokens page."
    insights:
      - "No long-lived token is stored anywhere in the repo; the OAuth session is cached locally. When a deploy fails, check `wrangler whoami` before touching any secret."
      - "Deploys and secret writes are per-account, so a whoami mismatch explains most sudden 403s."
    links:
      - label: Cloudflare dashboard
        url: https://dash.cloudflare.com/

  - id: r2-cdn-token
    title: Rotate the R2 API token behind the CDN publish
    service: Cloudflare R2
    applies_to: api-key
    signals:
      - "`upload-assets.js` fails, or the cdn repo's Actions publish run fails"
    steps:
      - "Decode the error first; most failures are not a dead credential. `Unauthorized`: token dead or revoked. `SignatureDoesNotMatch`: secret mis-pasted (R2 secrets are exactly 64 hex chars, check the length). `code 9109 Cannot use the access token from location`: client IP allowlist rejection, not a credential problem."
      - "To rotate: Cloudflare dashboard, R2, Manage R2 API Tokens. One R2 API token issues both the S3 key pair and the Cloudflare API token; they share one lifecycle."
      - "Update the local `.env` in `projects/neorgon-cdn-site/` (`CLOUDFLARE_ACCOUNT_ID`, `R2_ACCESS_KEY_ID`, `R2_SECRET_ACCESS_KEY`)."
      - "Update the same values as GitHub Actions secrets on the cdn repo; the Actions pipeline publishes on push."
    verify: "Push to the cdn repo and watch the Actions publish run, or run `upload-assets.js` locally."
    revoke: "Delete the token under Manage R2 API Tokens; both halves die together."
    insights:
      - "The IP-filter trap (hit 2026-08-03): allowlisting the machine's IPv6 address breaks within hours, because IPv6 privacy extensions rotate the suffix. Allowlist the stable IPv4, or a CIDR block."
      - "The CI token must have no client IP allowlist at all: GitHub runners rotate IPs, so an allowlisted token fails every run with code 9109. Mint a separate unfiltered token for CI if the interactive one is filtered."

  - id: gh-cli
    title: Fix GitHub CLI auth and account switching
    service: GitHub
    applies_to: oauth-session
    signals:
      - "`gh repo create` fails with a permissions error"
      - "`gh` commands act as the wrong account"
    steps:
      - "See every logged-in account and which is active: `gh auth status`."
      - "Switch with `gh auth switch --user <name>`; log a missing account in with `gh auth login` (browser flow)."
      - "Repo creation in the fleet's org must run as the account that owns it; switch before `gh repo create`, not after it fails."
    verify: "`gh auth status` shows the intended account as active."
    revoke: "`gh auth logout --user <name>`, or revoke the device under GitHub Settings, Applications."
    insights:
      - "Two personas are logged in on this machine and only one has repo-create rights in the org; `gh auth status` first is cheaper than a failed create."
      - "git identity is separate from gh identity: commits need the right `user.name`/`user.email` per repo even when gh is on the right account."

  - id: github-ssh
    title: Verify SSH push access to GitHub
    service: GitHub
    applies_to: ssh-key
    signals:
      - "`git push` asks for credentials or is refused"
    steps:
      - "Test the key: `ssh -T git@github.com` (expect the greeting with the right username)."
      - "If refused, check the agent has the key loaded: `ssh-add -l`."
      - "Remotes should use the SSH form (`git@github.com:owner/repo.git`); check with `git remote -v`."
    verify: "`ssh -T git@github.com` greets the expected account."
    revoke: "Remove the key under GitHub Settings, SSH and GPG keys."
    insights:
      - "SSH pushes work from agent sessions since 2026-08-18; the old HTTPS token workaround is retired. If a session claims it cannot push, the remote URL form is the first suspect."

  - id: namecheap-dns
    title: Restore Namecheap DNS API access
    service: Namecheap
    applies_to: ip-whitelist
    signals:
      - "DNS tooling fails with an API error after changing networks"
    steps:
      - "The API is IP-whitelisted and breaks on every network change, not only first setup. Check what IP you present: `make -C tooling/dns whoami` (run it in the current shell)."
      - "Add that IP at Namecheap, Profile, Tools, API Access."
      - "The key itself lives in a gitignored `.env` (`NAMECHEAP_API_KEY`); it does not rotate with the IP."
    verify: "Re-run the DNS command that failed; then commit the fresh zone backup the tooling writes."
    revoke: "Disable API access on the same Namecheap page."
    insights:
      - "Treat the whitelist as the credential: the key survives network changes, the allowlisted IP does not."
      - "After any DNS change, commit the zone backup file; its git history is the zone's only portable backup."

  - id: steam-web-api
    title: Obtain or rotate a Steam Web API key
    service: Valve Steam
    applies_to: api-key
    signals:
      - "The Steam-backed endpoint answers NOT_CONFIGURED or upstream 401s"
    steps:
      - "Request the key at steamcommunity.com/dev/apikey with a non-limited Steam account (Valve lifts the limit only after USD 5 spent from the store)."
      - "The form's domain field is an identifier, not a technical binding."
      - "Store it as a worker secret, never in a file: `wrangler secret put STEAM_API_KEY` from the worker directory, then `wrangler deploy`."
    verify: "Call the worker endpoint from outside; expect a JSON envelope, with NOT_CONFIGURED meaning the secret did not land."
    revoke: "The same page has Revoke My Steam Web API Key. Revoke immediately if the key ever appears in a commit, a log or a paste, then re-put the secret."
    insights:
      - "A bare `Forbidden` from the endpoint means the old worker is still deployed, not that the key is bad."
      - "Registering or revoking may require confirming in the Steam Mobile app."
    links:
      - label: Steam API key page
        url: https://steamcommunity.com/dev/apikey

  - id: github-enforcer-token
    title: Renew the github-enforcer GitHub token
    service: GitHub
    applies_to: api-key
    signals:
      - "`make -C tooling/github-enforcer check` stops with 401 Bad credentials"
      - "The enforcer warns at startup that the token is not 40 or 93 characters long"
      - "Repo policy drift stops being reported, so descriptions, homepages and topics quietly stop being enforced across the fleet"
    steps:
      - "Read the length before minting anything. A classic PAT is exactly 40 characters and a fine-grained one is 93. Any other length means the line in .env is malformed and the credential may be perfectly alive, so repair the line first and mint nothing."
      - 'Confirm the token itself is dead before replacing it: curl -sS -o /dev/null -w "%{http_code}" -H "Authorization: Bearer $GITHUB_TOKEN" https://api.github.com/user . A 200 means the token is alive and the fault is elsewhere; a 401 means it is genuinely gone.'
      - "Mint the replacement at github.com/settings/tokens. A classic token needs the `repo` scope. A fine-grained token needs, on the energon-a-secas repos: Administration read and write, Contents read and write, Metadata read, Pages read and write, Pull requests read and write."
      - "Install it by replacing the whole GITHUB_TOKEN line in `tooling/github-enforcer/.env`. Paste it as one line with nothing after it: no trailing comment, no wrapped remainder, no second value."
      - "Restrict the file: `chmod 600 tooling/github-enforcer/.env`. Gitignored is not the same as private, and this token can delete repositories."
      - "Record the new expiry date on the inventory entry github-enforcer-token, so the reminder fires before the next failure instead of after it."
    verify: "`make -C tooling/github-enforcer check-repo REPO=energon-a-secas/neorgon-site` prints all checks passed, with no length warning at startup."
    revoke: "Delete it under github.com/settings/tokens. A classic token is not scoped to a repo, so it dies everywhere at once."
    insights:
      - "401 Bad credentials does not mean expired. On 2026-09-05 the .env line held a valid 40-character classic PAT with 75 characters of pasted text stuck to the end, so the whole 115-character string went out and GitHub rejected it. The token had been alive the entire time, and this inventory had already recorded the credential as dead and told a future reader to mint a replacement. Check the length before believing the 401."
      - "The enforcer now says this itself: it warns at startup on a wrong-length value, and on a 401 it prints malformed rather than expired whenever the length matches no documented shape."
      - "Prefer a fine-grained token. A classic one carries every scope you tick across every repo the account can reach, delete_repo included, which is a wide blast radius for a tool whose job is editing descriptions and topics."
      - "Pages read and write is the permission most easily left off, and its absence used to pass as success: GitHub answers 404 rather than 403 for a Pages endpoint the token cannot see, which is the same answer as Pages being switched off. The enforcer now separates the two and warns when a repo has Pages enabled but the token cannot read it."
    links:
      - label: GitHub personal access tokens
        url: https://github.com/settings/tokens
      - label: Permissions required for fine-grained tokens
        url: https://docs.github.com/en/rest/authentication/permissions-required-for-fine-grained-personal-access-tokens

  - id: reddit-data-api-app
    title: Obtain or reuse a Reddit Data API app (Bouquin)
    service: Reddit
    applies_to: api-key
    signals:
      - "bouquin.neorgon.com reports its last scan came from the archive, not from Reddit"
      - "`npx convex run ingest:scan --prod` returns status skipped, or a token error mentioning 401"
      - "Reddit's app-registration deadline (2026-09-30) is approaching and an existing app is not registered"
    steps:
      - "Know what changed before touching anything. Reddit closed self-service Data API registration on 2025-11-11 (r/redditdev post 1oug31u, 'Introducing the Responsible Builder Policy'); every new OAuth app is approved by a person, and personal read-only requests reportedly wait weeks. The site does not depend on this: `convex/ingest.ts` reads the Arctic Shift archive until the env vars exist."
      - "Check for an app you already own: log in and open reddit.com/prefs/apps. An app of type script or web app listed under 'developed applications' shows its client id under its name and its secret in the edit view. That is the fast path."
      - "If one exists, register it at developers.reddit.com/app-registration before 2026-09-30 (asked for in the 2026-08-05 admin post 1vgbm9c, 'to remain in good standing'). Describe it as personal, non-commercial, read-only, one subreddit, about 60 requests per hour."
      - "If none exists, file the access request at support.reddithelp.com/hc/en-us/requests/new?ticket_form_id=14868593862164 (the link Reddit's own 'Accessing Reddit Data' article gives for non-commercial use), and note the date on this inventory entry. Trying 'create another app' on prefs/apps costs nothing, but r/redditdev has reported that form resetting silently or failing with a 500 since July 2026."
      - "With an id and secret in hand, run `bash scripts/setup-reddit.sh` from `projects/bouquin-site`. It captures the id, the secret (hidden), and your username for the User-Agent `web:com.neorgon.bouquin:v1.0 (by /u/NAME)`, writes them to the gitignored `.env`, and runs `npx convex env set` for REDDIT_CLIENT_ID, REDDIT_CLIENT_SECRET and REDDIT_USER_AGENT on the deployment you choose (prod, dev or both)."
      - "Nothing else changes: the next cron run picks the Reddit source by itself because `credsFromEnv()` now returns a value."
    verify: "`npx convex run ingest:scan --prod '{\"maxThreads\": 3}'` returns status ok and source reddit, and the stats line under the site's hero says the last scan came from Reddit."
    revoke: "Delete the app at reddit.com/prefs/apps (the id and secret die together), then `npx convex env remove REDDIT_CLIENT_ID REDDIT_CLIENT_SECRET REDDIT_USER_AGENT --prod`; the next scan falls back to the archive with no other change."
    insights:
      - "The credential is optional for the site and only improves it: live scores and comment counts instead of the archive's 16-minute lag and 36-hour score refresh. Do not block a launch on it."
      - "App-only OAuth (grant_type=client_credentials) is all the cron uses, so no Reddit password ever reaches Convex; the token endpoint answers 401, not 403, from outside Reddit, which is how you tell a dead credential from a network block. The unauthenticated .json endpoints answer 403 to everything now and the RSS feed is limited to one request per minute, so neither is a fallback worth building."
      - "Reddit blocks generic User-Agents at the network edge. The 403 that produces looks like a credential failure and is not one; check the User-Agent before rotating anything."
      - "Token lifetime is 24 hours and the cron caches it in the kv table; a 401 on a read drops the cached token so the next run mints a fresh one. A 401 on the token endpoint itself is the id or secret."
      - "Two deadlines live on this entry: the 2026-09-30 registration of any existing app, and, after that, Reddit's stated plan to move third-party apps onto its Developer Platform 'gradually' with nothing changing in 2026. Re-read the r/redditdev admin posts before the next renewal rather than this note."
    links:
      - label: Your Reddit apps
        url: https://www.reddit.com/prefs/apps
      - label: App registration (deadline 2026-09-30)
        url: https://developers.reddit.com/app-registration
      - label: Data API access request form
        url: https://support.reddithelp.com/hc/en-us/requests/new?ticket_form_id=14868593862164
      - label: Responsible Builder Policy
        url: https://support.reddithelp.com/hc/en-us/articles/42728983564564-Responsible-Builder-Policy
      - label: Research report (fleet)
        url: https://github.com/energon-a-secas/bouquin-site

  - id: clerk-instance
    title: Recover or promote the Clerk instance
    service: Clerk
    applies_to: other
    signals:
      - "Sign-in fails on a Clerk-backed site, or Convex rejects the JWT as an untrusted issuer"
      - "You need the publishable key for a new site and cannot remember where it comes from"
      - "Sign-ups stop working after 100 accounts"
    steps:
      - "You almost never need the dashboard. The publishable key is PUBLIC by design and already ships in the fleet: copy the `clerk-publishable-key` meta tag from an existing site's index.html (memes, character-sheet or buyhacks)."
      - "The issuer domain is not stored separately anywhere: it is base64 inside the key. Decode the part after `pk_test_` or `pk_live_` and strip the trailing `$` to get the Frontend API host."
      - "Every Convex deployment must set the same host as `CLERK_JWT_ISSUER_DOMAIN`, and each app's convex/auth.config.ts must name it. A site whose config disagrees will reject every token."
      - "For a NEW key, or to promote: dashboard.clerk.com, then API keys for the key, or Domains to create a production instance. Promotion needs a domain you own and five DNS records it will show you, which are per-instance and not knowable in advance."
    verify: "Sign in on the site, then confirm a Convex function that calls `ctx.auth.getUserIdentity()` returns a subject rather than null."
    revoke: "Rotating the publishable key means updating every site's meta tag and every Convex issuer at once; there is no partial cutover. Clerk documents that the publishable key does not need rotating even though it is public."
    insights:
      - "A development instance is capped at 100 users and its users CANNOT be moved to a production instance. Every account created before promotion is lost, and Convex rows keyed by the Clerk subject are orphaned when new user ids are issued. Promote before inviting anyone, not after."
      - "A production instance is free on Clerk's Hobby plan. Pro is forced only by paid features, none of which the fleet uses."
      - "Promotion changes the Frontend API host from the accounts.dev one to a subdomain of your own domain, so every Content-Security-Policy naming the old host stops matching. Grep the fleet before cutting over; it appears far more widely than the sites that actually run Clerk."
      - "If the domain already sends mail, Clerk's SPF record must be MERGED into the existing one. RFC 7208 allows exactly one v=spf1 record per domain, and adding a second breaks mail delivery outright."
      - "A stalled certificate at promotion is usually a CAA record on the domain; an empty `dig <domain> CAA` means you are clear."
    links:
      - label: Clerk dashboard
        url: https://dashboard.clerk.com/
      - label: Deploy to production
        url: https://clerk.com/docs/guides/development/deployment/production
      - label: Development vs production instances
        url: https://clerk.com/docs/guides/development/managing-environments

  - id: google-oauth-clerk
    title: Google sign-in for a Clerk production instance
    service: Google Cloud
    applies_to: api-key
    signals:
      - "The Google button returns Google's own error: 'Missing required parameter: client_id', Error 400 invalid_request"
      - "Google sign-in works for you and silently fails for everyone else"
      - "Clerk's OAuth verification refuses the consent screen over the privacy policy or terms URL"
    steps:
      - "Confirm the cause from a terminal before touching a dashboard. POST an anonymous sign-in attempt to the Frontend API and read the Google URL back: the client_id is empty when no credential is configured. Same command verifies the fix afterwards."
      - "Set the Clerk Dashboard's instance switcher to Production first. Dashboard links resolve to whichever instance you were last in, and configuring development by mistake is silent: development works on Clerk's shared credentials, so nothing errors and production stays broken."
      - "Clerk Dashboard, SSO connections (this page merged the old 'Social Connections' and 'Enterprise Connections' in October 2024, so older tutorials name screens that no longer exist). Select the existing Google connection rather than adding one, then turn on 'Use custom credentials'."
      - "Copy the Authorized Redirect URI the panel then shows, and leave that tab open."
      - "Google Cloud Console, APIs and Services, Credentials, Create Credentials, OAuth client ID, application type Web application. Paste Clerk's redirect URI into Authorized redirect URIs, and the site origin into Authorized JavaScript origins."
      - "Create. Google shows the client id and client secret once. Copy both now."
      - "Back in Clerk, paste both into the connection and save. Leave the Scopes field empty: Clerk already sends what sign-in needs, and that field is for additions."
      - "In Google Cloud, set the OAuth app's publishing status to 'In production'."
    verify: "Re-run the sign-in probe and confirm client_id is no longer empty, then sign in through the Account Portal rather than your own frontend, which removes your site from the equation."
    revoke: "Delete the OAuth client in Google Cloud Console, Credentials. Sign-in with Google breaks immediately for everyone, so create the replacement first if you are rotating rather than retiring."
    insights:
      - "A DEVELOPMENT Clerk instance uses Clerk's own shared Google credentials; a PRODUCTION one refuses to. That is why the button works for months and breaks the day you promote, with an error that points at Google rather than at Clerk."
      - "THE CLIENT SECRET IS SHOWN ONCE. There is no way to read it again later. Losing it means creating a new client, not recovering the old one."
      - "Publishing status 'Testing' caps the app at 100 users and only lets explicitly listed test users in. It will work perfectly for whoever set it up and fail for everyone else, with no error anyone can see. This is the single easiest thing to leave half-done."
      - "The three scopes sign-in needs (openid, userinfo.email, userinfo.profile) are Google's basic non-sensitive ones, so going to production triggers no verification review. Asking for anything more does."
      - "Verification still checks the consent screen's links: the homepage must link the privacy policy, the policy URL must respond, and the page must actually describe what is collected. A stub page fails on the third count even when the first two pass."
      - "No billing account is needed. Creating an OAuth client and using it for sign-in is free."
    links:
      - label: Google Cloud credentials
        url: https://console.cloud.google.com/apis/credentials
      - label: Clerk SSO connections
        url: https://dashboard.clerk.com/last-active?path=user-authentication/sso-connections
      - label: Clerk's Google social connection guide
        url: https://clerk.com/docs/guides/configure/auth-strategies/social-connections/google

  - id: vitrina-clerk-webhook
    title: Connect or rotate the Vitrina Clerk webhook secret
    service: Clerk
    applies_to: secret
    signals:
      - "A Neorgon account was deleted in Clerk and its Vitrina profile or books are still in the production `profiles` or `entries` table"
      - "The endpoint's Message Attempts in the Clerk Dashboard show 503 (`CLERK_WEBHOOK_SECRET` is not set on the deployment the endpoint URL names, for example because it was set on dev instead) or 400 (the value there is not this endpoint's signing secret)"
      - "Svix reports the endpoint as disabled"
      - "The inventory row vitrina-clerk-webhook-secret still says pending, while neorgon.com/privacy already promises that deleting a Neorgon account erases its Vitrina data, normally within a day"
    steps:
      - "Find the deployment first: `make -C projects/vitrina-site where` prints the production URL the pages call, `https://fantastic-chickadee-557.convex.cloud` on 2026-09-14. The webhook goes to the same name on the `.convex.site` host: `https://fantastic-chickadee-557.convex.site/clerk-users-webhook`. The `.convex.cloud` host does not serve HTTP actions, and the `CONVEX_DEPLOYMENT` line `where` also prints is the dev deployment, not this one."
      - "In the Clerk Dashboard, set the instance switcher to Production, open Webhooks and select Add Endpoint. Dashboard links open whichever instance you used last, and an endpoint on the development instance never hears about a production deletion."
      - "Paste the URL into Endpoint URL, select only `user.deleted` under Message Filtering, and select Create. Clerk opens the endpoint's settings page; leave it open."
      - "Reveal and copy the Signing Secret on that page, `whsec_` prefix included."
      - "Set it on production in the same sitting, from `projects/vitrina-site` with the secret on the clipboard: `pbpaste | npx convex env set --prod CLERK_WEBHOOK_SECRET`. Piping keeps it out of shell history. The Convex dashboard works too: project vitrina-site, the Production deployment, Settings, Environment Variables."
      - "Check it landed without printing it: `npx convex env get --prod CLERK_WEBHOOK_SECRET | tr -d '\\n' | shasum -a 256`, then `pbpaste | tr -d '\\n' | shasum -a 256`. The hashes must match. Clear the clipboard afterwards with `pbcopy < /dev/null`. Never `npx convex env list`: it prints every value on the deployment."
      - "On the endpoint page's Testing tab, send an example `user.deleted` and confirm Message Attempts shows 200 (Succeeded)."
      - "Update the inventory row vitrina-clerk-webhook-secret: status active, and a created date."
    verify: "The example `user.deleted` shows 200 in the endpoint's Message Attempts, and the two hashes match. A hash beginning `e3b0c442` is the hash of empty input: the variable is not set on the deployment you asked."
    revoke: "Clerk's documented rotation adds a second endpoint with the same URL and the `user.deleted` filter, sets its secret as `CLERK_WEBHOOK_SECRET`, checks it the same way, then deletes the old endpoint from its ... menu. While both exist the old endpoint's deliveries answer 400 and are retried; deleting it stops them. To retire the webhook, delete the endpoint first and only then run `npx convex env remove --prod CLERK_WEBHOOK_SECRET`; in the other order the endpoint spends 5 days failing with 503 before Svix disables it."
    insights:
      - "A 200 proves the secret by itself. The handler answers 503 while `CLERK_WEBHOOK_SECRET` is unset; 400 when the `svix-id`, `svix-timestamp` or `svix-signature` header is missing, the timestamp is more than 300 seconds from the deployment's clock, or the signature does not verify; and 200 only after verification. A 400 on the example event is the value or the deployment it went to, so compare the hashes before suspecting the code."
      - "Only a verified `user.deleted` whose `data.id` matches `^user_[A-Za-z0-9]+$` erases anything, through `purge:eraseSubject`, the same path Delete my Vitrina data takes. Any other verified event, and signed JSON that is not an event, is answered 200 and ignored, so a filter left wider than `user.deleted` costs deliveries, never data."
      - "The example event is safe on production. It names a user id no Vitrina account has, so the erasure it starts finds no profile and no books, and `purge:sweep` deletes its `erasures` row about 5 minutes later. A second example inside those 5 minutes is answered 200 and starts nothing."
      - "Failures are retried, not lost. Svix retries after 5 seconds, 5 minutes, 30 minutes, 2 hours, 5 hours, then 10 hours twice, and disables an endpoint that keeps failing for 5 days. The 503 while the secret is unset is deliberate: a deletion that lands between Create and setting the secret is delivered again once it is set. An erasure that throws answers 500 and is retried the same way, and erasing twice is safe."
      - "`npx convex env get` ends its output with a newline and `pbpaste` does not. Without `tr -d '\\n'` on both sides the hashes never match, and a correct secret looks wrong."
      - "A webhook signing secret does not expire, so the inventory row says `expires: never`. What goes stale is the pairing: every new endpoint (a rotation, or one recreated after a delete) has a new secret, and the old value on the deployment then fails every delivery with 400."
      - "If the endpoint page offers to rotate the secret in place instead, Svix keeps signing with the old secret as well for 24 hours by default, sending one `v1` signature per secret. Vitrina's `convex/lib/webhookVerify.ts` accepts any one that verifies, so there is no gap as long as the new value is set within that day."
    links:
      - label: Clerk webhooks
        url: https://dashboard.clerk.com/~/webhooks
      - label: Rotate a webhook signing secret (Clerk)
        url: https://clerk.com/docs/guides/secure/rotate-api-keys
      - label: Svix retry schedule
        url: https://docs.svix.com/retries
      - label: Convex dashboard
        url: https://dashboard.convex.dev/

  - id: vitrina-admin-subjects
    title: Set the Vitrina admin ids (ADMIN_SUBJECTS)
    service: Convex
    applies_to: other
    signals:
      - "An `admin:` function on Vitrina answers `not-admin` for you"
      - "Public shelves are about to open: `npx convex env set --prod PUBLISHING open` must not run before this list is set"
      - "A shelf has to be hidden or erased (an abuse report, or an erasure request for an account that is already gone) and nobody is listed to do it"
    steps:
      - "Copy your Clerk user id from the PRODUCTION instance: Clerk Dashboard, instance switcher on Production, Users, select the account, copy the id that starts with `user_`. An id from the development instance matches nobody on production, because Clerk users do not move between instances."
      - "Confirm the deployment: `make -C projects/vitrina-site where` prints the production URL the pages call (`fantastic-chickadee-557` on 2026-09-14). In the Convex dashboard it is the Production deployment of project vitrina-site."
      - "Set the list from `projects/vitrina-site`: `npx convex env set --prod ADMIN_SUBJECTS user_...`. Several admins are one comma-separated value, `user_a,user_b`, and setting it again replaces the whole list. The dashboard works too: the Production deployment, Settings, Environment Variables."
      - "Check it landed without listing the deployment: `npx convex env get --prod ADMIN_SUBJECTS | tr -d '\\n' | shasum -a 256` against `printf '%s' 'user_...' | shasum -a 256` over exactly the text you set. The value is not a secret, but `npx convex env list` would print the secrets stored beside it."
      - "Only after this, and after the decision to open publishing, run `npx convex env set --prod PUBLISHING open`."
    verify: "The two hashes match. To prove the gate itself, call `admin:releaseHandle` as your subject with a handle nobody holds: `npx convex run --prod --identity '{\"subject\":\"user_...\",\"issuer\":\"https://clerk.neorgon.com\"}' admin:releaseHandle '{\"handle\":\"no-such-hold\"}'`. A listed id gets ok true with released false, anyone else gets code not-admin, and nothing changes either way."
    revoke: "Set the list again without the id; `npx convex env remove --prod ADMIN_SUBJECTS` leaves nobody an admin, which is safe. No deploy is needed, because the handlers read `process.env.ADMIN_SUBJECTS` on every call. Deleting someone's Clerk account stops them signing in but leaves their id listed here, so remove it in both places."
    insights:
      - "The list gates five mutations: `admin:setSuspended` and `admin:purgeByHandle` by handle; `admin:setSubjectSuspended` and `admin:purgeBySubject` by Clerk user id, for a shelf that never had a handle or a suspended person who already erased their data; and `admin:releaseHandle`, which lifts a 30-day hold early. Each checks the caller before reading anything, so an unlisted caller, signed out included, gets `not-admin` and learns nothing about whether a handle exists."
      - "An unset or empty list means nobody is an admin, never everybody. Matching is exact against the Convex token's subject, which is the Clerk user id; spaces around commas and empty items are ignored, and an email or a username matches nobody. `user_a, user_b` works the same as `user_a,user_b` but hashes differently, so a hash mismatch can be spacing rather than a wrong id."
      - "There is no admin page. Vitrina's `js/backend.js` names the five functions and nothing calls them; an admin runs them as a listed subject, from the command line as in the verify step, or from the Convex dashboard's function runner with Act as a user."
      - "The gate trusts `ctx.auth`. A browser cannot forge a subject, but anyone with deploy access to the Convex project can act as any subject with `--identity` or Act as a user, so the real set of admins is this list plus the Convex team."
      - "Never put a synthetic id on production. Dev lists `user_vitrina_dev_admin` for Vitrina's `scripts/convex_smoke.py --dev`, and the production smoke run refuses to start if its own subject, `user_vitrina_smoke`, is listed, because it ends by erasing everything that subject owns."
      - "Publishing ships closed, and this list is the one setting it needs first. Once shelves are public, hiding a reported one or honouring an erasure request needs a listed admin, and the privacy and terms pages already say the operator can do both."
    links:
      - label: Clerk users
        url: https://dashboard.clerk.com/~/users
      - label: Convex dashboard
        url: https://dashboard.convex.dev/
      - label: Acting as a user in the Convex dashboard
        url: https://docs.convex.dev/dashboard/deployments/functions
